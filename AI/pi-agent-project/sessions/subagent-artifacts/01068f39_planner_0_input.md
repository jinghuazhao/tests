# Task for planner

Task: Refactor a CSV statistics script into a clean CLI Python package.

Requirements:
- modular design
- CLI entry point
- safe CSV parsing
- compute mean + median
- robust error handling

Input file: data.csv (single column "value" with numbers)

Create a plan with:
- Numbered implementation steps
- Required files
- Dependency notes and risks (parsing, IO, CLI issues)